import React from "react";

import GetRockets from "components/Rockets";

const RocketScreen: React.FunctionComponent = (props) => {
  return <GetRockets />;
};

export default RocketScreen;
